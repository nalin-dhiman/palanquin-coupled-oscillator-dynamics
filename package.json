{
  "name": "rath-dynamics-lab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Interactive finite-angle two-support heave and roll dynamics",
  "scripts": {"build": "node scripts/build.mjs", "test": "node --test tests/*.test.mjs", "start": "python3 -m http.server 8080 --directory dist", "deploy": "node scripts/deploy.mjs"},
  "dependencies": {"three": "0.180.0"},
  "devDependencies": {"@playwright/test": "1.57.0"},
  "engines": {"node": ">=18"}
}
